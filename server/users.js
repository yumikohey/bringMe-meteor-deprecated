Meteor.publish("bringme_users", function (user) {
  // var user = Meteor.users.insert({email:user.email, username:user.username, password:user.password});
  // return Meteor.users.find({user:user});
});